export enum PageEndpoints {
  home = '/',
  login = '/auth/login',
  resetPassword = '/auth/reset-password',

  profile = '/profile',
  forum = '/forum',
  directory = '/directory',
  jobs = '/jobs',
  events = '/events'
}
